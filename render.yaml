services:
  - type: web
    name: gear-optimizer
    runtime: python
    buildCommand: ""
    startCommand: streamlit run app.py --server.port $PORT
    envVars:
      - key: PORT
        value: 10000